#!/usr/bin/env bash

if [[ $1 == "install" ]]
then
    if [[ -f models.db ]]
    then
        rm models.db
    fi
    pip install --user -r dependencies.txt && poetry install
elif [[ $1 == "test" ]]
then
    python test_runner.py
else
    python src/__main__.py analyze "$1"
fi